export const getRunning=async()=>({id:'r1',subject_type:'task',subject_id:'t3',label:'Unable to add sub account',started_at:new Date(Date.now()-24*60e3-11e3).toISOString()});export const startTimer=async()=>{};export const stopTimer=async()=>{};export const fmtClock=(s)=>{s=Math.max(0,Math.round(s));const h=Math.floor(s/3600),m=Math.floor((s%3600)/60),sec=s%60;return `${h}:${String(m).padStart(2,'0')}:${String(sec).padStart(2,'0')}`;};export const fmtDuration=(s)=>{s=Math.max(0,Math.round(s));const h=Math.floor(s/3600),m=Math.floor((s%3600)/60);return h?`${h}h ${String(m).padStart(2,'0')}m`:`${m}m`;};